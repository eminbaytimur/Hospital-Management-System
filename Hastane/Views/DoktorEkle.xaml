<UserControl x:Class="Hastane.Views.DoktorEkle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Hastane.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:Background="{StaticResource MaterialDesignLightSeparatorBackground}"
             d:DesignHeight="600" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBox x:Name="txtKimlikNo" MaxLength="11" PreviewTextInput="txtKimlikNo_PreviewTextInput" materialDesign:HintAssist.Hint="Kimlik No" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="{StaticResource CreateElementMargin}" Padding="{StaticResource CreateElementPadding}" />
            <TextBox x:Name="txtAd" MaxLength="50" materialDesign:HintAssist.Hint="Ad" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="{StaticResource CreateElementMargin}" Padding="{StaticResource CreateElementPadding}" />
            <TextBox x:Name="txtSoyad" MaxLength="50" materialDesign:HintAssist.Hint="Soyad" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="{StaticResource CreateElementMargin}" Padding="{StaticResource CreateElementPadding}" />
            <DatePicker x:Name="dpDogumTarihi" materialDesign:HintAssist.Hint="Dogum Tarihi" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" Margin="{StaticResource CreateElementMargin}" Padding="{StaticResource CreateElementPadding}" />
            <ComboBox x:Name="cmbCinsiyet" materialDesign:HintAssist.Hint="Cinsiyet" Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="{StaticResource CreateElementMargin}" Padding="{StaticResource CreateComboPadding}" SelectionChanged="cmbCinsiyet_SelectionChanged" />
            <TextBox x:Name="txtTelefon" MaxLength="12" materialDesign:HintAssist.Hint="Telefon (xxx-xxx-xxxx)" TextChanged="txtTelefon_TextChanged" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="{StaticResource CreateElementMargin}" Padding="{StaticResource CreateElementPadding}" />
            <TextBox x:Name="txtEposta" MaxLength="100" materialDesign:HintAssist.Hint="Eposta" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="{StaticResource CreateElementMargin}" Padding="{StaticResource CreateElementPadding}" />
            <ComboBox x:Name="cmbBolum" materialDesign:HintAssist.Hint="Bolum" Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="{StaticResource CreateElementMargin}" Padding="{StaticResource CreateComboPadding}" />
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right">
                <Ellipse Width="100" Height="100" Margin="10">
                    <Ellipse.Fill>
                        <ImageBrush x:Name="imgDoktor" Stretch="UniformToFill" />
                    </Ellipse.Fill>
                </Ellipse>
                <Button x:Name="btnSelectImg" Content="{materialDesign:PackIcon Kind=InsertPhoto}" VerticalAlignment="Top" Style="{StaticResource MaterialDesignIconButton}" Click="btnSelectImg_Click" />
            </StackPanel>            
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="btnKaydet" Content="Kaydet" Width="80" Style="{StaticResource MaterialDesignRaisedDarkButton}" Margin="2,8" Click="btnKaydet_Click" />
            <Button x:Name="btnIptal" Content="Iptal" Width="80" Style="{StaticResource MaterialDesignRaisedSecondaryDarkButton}" Foreground="White" Margin="2,8" Click="btnIptal_Click" />
        </StackPanel>
    </Grid>
</UserControl>