<Page x:Class="Hastane.Views.HastaDetay"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Hastane.Views"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HastaDetay">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="txtId" materialDesign:HintAssist.Hint="Hasta Id" Style="{StaticResource MaterialDesignOutlinedTextBox}" />
                <Button Grid.Column="1" Width="100" Margin="32,4" Content="Sorgula" x:Name="btnSorgula" HorizontalAlignment="Right" Style="{StaticResource MaterialDesignRaisedSecondaryLightButton}" Click="btnSorgula_Click" />
            </Grid>

            <Border Grid.Row="1" BorderThickness="4" BorderBrush="{StaticResource MaterialDesign.Brush.Primary.Dark}" />

            <Expander x:Name="exRandevu" Grid.Row="2" Margin="4,10" Header="Randevu Kaydi" IsExpanded="True">
                <DataGrid x:Name="dgRandevular" IsReadOnly="True" />
            </Expander>

            <Border Grid.Row="3" BorderThickness="4" BorderBrush="{StaticResource MaterialDesign.Brush.Primary.Dark}" />

            <Expander x:Name="exRecete" Grid.Row="4" Margin="4,10" Header="Recete Kaydi" IsExpanded="True">
                <DataGrid x:Name="dgReceteler" IsReadOnly="True" />
            </Expander>

            <Border Grid.Row="5" BorderThickness="4" BorderBrush="{StaticResource MaterialDesign.Brush.Primary.Dark}" />

            <Expander x:Name="exAmeliyat" Grid.Row="6" Margin="4,10" Header="Ameliyat Kaydi" IsExpanded="True">
                <DataGrid x:Name="dgAmeliyatlar" IsReadOnly="True" />
            </Expander>

            <Border Grid.Row="7" BorderThickness="4" BorderBrush="{StaticResource MaterialDesign.Brush.Primary.Dark}" />

            <Expander x:Name="exTest" Grid.Row="8" Margin="4,10" Header="Tahlil Kaydi" IsExpanded="True">
                <DataGrid x:Name="dgTestler" IsReadOnly="True" />
            </Expander>
        </Grid>
    </ScrollViewer>
</Page>
