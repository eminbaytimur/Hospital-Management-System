<Window x:Class="Hastane.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hastane"
        xmlns:views="clr-namespace:Hastane.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        mc:Ignorable="d"
        Title="MainWindow" Height="750" Width="1200">

    <Grid>
        <!-- Left Panel - Navbar -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="4">
            <Rectangle Height="20" />
            <Button x:Name="btnSorgu" Content="Sorgu" Height="50" Style="{StaticResource MaterialDesignFlatButton}" Click="Sorgu_Click"/>
            <Button x:Name="btnKayit" Content="Kayit" Height="50" Style="{StaticResource MaterialDesignFlatButton}" Click="Kayit_Click"/>
            <Button x:Name="btnHastaIslemleri" Content="Hasta Islemleri" Height="50" Style="{StaticResource MaterialDesignFlatButton}" Click="HastaIslemleri_Click"/>
            <Button x:Name="btnHastaDetay" Content="Hasta Detay" Height="50" Style="{StaticResource MaterialDesignFlatButton}" Click="HastaDetay_Click"/>
        </StackPanel>

        <Border Grid.Column="1" BorderThickness="4" BorderBrush="{StaticResource MaterialDesign.Brush.Primary.Dark}" />

        <!-- Right Panel - Content -->
        <Frame Name="ContentFrame" NavigationUIVisibility="Hidden" Grid.Column="2" Margin="4"/>
    </Grid>
</Window>
